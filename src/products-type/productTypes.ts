export interface Product {
    id: string; 
    productName: string;
    price: number;
    imageUrl: string;
  }
  
  
  export interface Carousel {
    id: string; 
    imageUrl: string;
    redirectUrl: string;
  }